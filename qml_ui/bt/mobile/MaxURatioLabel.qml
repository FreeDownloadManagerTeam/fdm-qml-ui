import QtQuick
import org.freedownloadmanager.fdm
import "../../mobile/SettingsPage"

SettingsGroupHeader {
    id : maxURatioGroup
    name: App.my_BT_qsTranslate("SettingsPage", "Stop seeding at ratio") + App.loc.emptyString
}
